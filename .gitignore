# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# tsc -b build artifacts (vite.config.ts has no separate outDir, so `tsc -b`
# emits its compiled output right next to the source — these are byproducts
# of `npm run build`, not source, and get regenerated every build).
vite.config.js
vite.config.js.map
vite.config.d.ts
vite.config.d.ts.map
*.tsbuildinfo

# Editor/OS
.DS_Store
*.local

# Env files
.env
.env.*
!.env.example

# Personal files (not project code)
Pivak E Safa.pdf
profile pic.jpg
