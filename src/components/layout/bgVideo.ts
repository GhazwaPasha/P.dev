/** Playback rate for the site-wide ambient background video (both the fixed
 *  PageShell layer in BackgroundBlobs and Home's duplicate glass-backdrop
 *  copy) — kept in one place so the two stay in sync. 1 = native speed. */
export const BG_VIDEO_PLAYBACK_RATE = 1;
